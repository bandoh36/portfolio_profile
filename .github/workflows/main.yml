name: Upload files to S3

on:
  push:
    branches:
    - main

env:
  MY_AWS_REGION: 'ap-northeast-1'
  AWS_ROLENAME: 'role_githubactions_s3fullaccess'
  AWS_S3_BUCKET: 'akisute-portfolio'
  SOURCE_DIR: 'build'

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
    - name: Checkout
      uses: actions/checkout@master
    - name: Configure AWS credentials with IAM Role
      uses: aws-actions/configure-aws-credentials@main
      with:
        role-to-assume: arn:aws:iam::${{secrets.AWS_ACCOUNT_ID}}:role/${{env.AWS_ROLENAME}}
        aws-region: ${{env.MY_AWS_REGION}}
    - name: Copy files to S3
      run: |
          npm run build
          aws s3 rm s3://${{env.AWS_S3_BUCKET}}/ --recursive '.*git*'
          aws s3 cp ${{env.SOURCE_DIR}} s3://${{env.AWS_S3_BUCKET}}/--recursive '.*git*'
